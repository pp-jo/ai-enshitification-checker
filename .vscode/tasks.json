{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "AI model sanity check",
      "type": "shell",
      "command": "uv run aimeter",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
